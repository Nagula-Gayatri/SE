In Windows CMD or PowerShell:
ssh -i myKey.pem ec2-user@44.192.91.8

Now your prompt should change to something like:
[ec2-user@ip-172-31-75-177 ~]$

Only after this continue.
Clone your repo (on EC2)
git clone <your-git-url>

Move into the repo folder
cd yourrepo

Create Dockerfile (on EC2)
nano Dockerfile

Paste:
FROM nginx
COPY index.html /usr/share/nginx/html

Save:
CTRL + O → ENTER
CTRL + X

Build Docker image
sudo docker build -t mywebapp .

Run container
sudo docker run -d -p 80:80 mywebapp

Open the website
http://44.192.91.8
